export enum TILE_STATUS {
  AVAILABLE = "AVAILABLE",
  SELECTED = "SELECTED",
  WHITE = "WHITE",
  BLACK = "BLACK",
}

export enum PLAYER_TURN {
  BLACK = "BLACK",
  WHITE = "WHITE,",
}

export enum BoardActionType {
  SELECT = "SELECT",
  DESELECT = "DESELECT",
  SIZE  = "SIZE",
  DATE = "DATE",
  WINNER = "WINNER",
  EMPTY = "EMPTY"
}
